import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#cdb4db', 
    },
    secondary: {
      main: '#003049', 
    },
    background: {
      default: '#edede9',
    },
  },
});

export default theme;