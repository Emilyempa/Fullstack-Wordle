import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#003049', 
    },
    secondary: {
      main: '#cdb4db', 
    },
    background: {
      default: '#edede9',
    },
  },
});

export default theme;